<%= simple_form_for [@teacher] do |f| %>
<%= f.input :name %>
<%= f.input :description %>
<%= f.association :subject, as: :select %>
<%= f.input :picture %>
<%= f.input :picture_cache, as: :hidden %>
<%= f.input :rate %>
<%= f.submit "Become a Teacher", class: "btn btn-primary"%>
<% end %>